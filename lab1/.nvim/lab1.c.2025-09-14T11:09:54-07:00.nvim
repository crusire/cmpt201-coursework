#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
  char *in = NULL;
  size_t buffersize = 0;
  printf("Please enter some text: ");
  ssize_t charnum = getline(&in, buffersize, stdin);
}
