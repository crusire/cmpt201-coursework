#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main(void) {
  printf("Hello World!\n");
  pid_t pid = fork(); //split into two functions
  
  if (pid = 0) { //as the child...
    printf("Hello I am the child!"\n);
    execl("/bin/ls", "-a", "-l", "-h", NULL);
  }
  else {
    printf("Hello I am the parent!\n");
    execl("/bin/ls", "-a", NULL);
  }
  printf("%d\n", getpid());
}
